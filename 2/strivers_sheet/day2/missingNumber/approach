//Missing number. 

approach 1. 

	1.given : nums[]
	2.make array arr of length nums.length+1 arr[nums.length+1]
	3. for each i in nums, 
		arr[nums[i]]=1;
	4. find first i in arr, where arr[i] = 0;
	5. if not found return arr.length; (as 0-n all are found, return n+1).


approach 2.
 
 	maths.
 	
 	sum of first n numbers -> n(n+1)/2;
 	calculate sum of array nums[];
 	expected_sum - sum ->answer;
 
 	//code.
        int sum = 0;
        for(int i:nums)
            sum+=i;
        int n = nums.length;
        int totalExpectedSum = n*(n+1)/2;
        return totalExpectedSum-sum;
